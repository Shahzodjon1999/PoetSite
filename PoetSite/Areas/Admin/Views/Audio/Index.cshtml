@model List<PoetSite.Models.AudioPoem>

@{
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2 class="mb-4">🎧 Шеърҳои аудиоӣ</h2>

<a class="btn btn-primary mb-3" href="/Admin/Audio/Create">➕ Иловаи аудио</a>

<table class="table table-bordered table-hover align-middle">
    <thead class="table-light">
    <tr>
        <th>Сарлавха</th>
        <th>Аудио</th>
        <th>Амалҳо</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Title</td>
            <td>
                <audio controls style="width: 250px">
                    <source src="@item.AudioPath" type="audio/mpeg" />
                </audio>
            </td>
            <td>
                <a class="btn btn-sm btn-warning"
                   href="/Admin/Audio/Edit/@item.Id">
                    ✏ Таҳрир
                </a>

                <a class="btn btn-sm btn-danger"
                   href="/Admin/Audio/Delete/@item.Id">
                    🗑 Нест кардан
                </a>
            </td>
        </tr>
    }
    </tbody>
</table>